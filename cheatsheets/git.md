Change user config (useful for switching between work and personal emails):
```sh
git config --local user.email me@email.com

# if you need to amend a previous commit:
git commit --amend --reset-author
```
